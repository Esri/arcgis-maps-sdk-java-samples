//[DocRef: Name=Use_Cloud_And_Servers-Access_ArcGIS_Platform-Credential
 UserCredential credential = new UserCredential(username, password);
    final Portal portal = new Portal("http://www.arcgis.com");
    portal.setCredential(credential);
    portal.addDoneLoadingListener(new Runnable() {
      @Override
      public void run() {
        if (portal.getLoadStatus() == LoadStatus.LOADED) {
          PortalUser user = portal.getUser();
          String userDisplayName = user.getFullName(); // Returns display name of authenticated user
        }
      }
    });
    portal.loadAsync();
//[DocRef: Name=Use_Cloud_And_Servers-Access_ArcGIS_Platform-Credential